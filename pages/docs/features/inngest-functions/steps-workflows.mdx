import { CardGroup, Card, ImageTheme, CodeGroup } from "src/shared/Docs/mdx";
import EventIcon from 'src/shared/Icons/Event';
import TypeScriptIcon from 'src/shared/Icons/TypeScript';
import PythonIcon from 'src/shared/Icons/Python';
import GoIcon from 'src/shared/Icons/Go';

import {
  RiGuideFill,
  RiCalendarLine,
  RiGitForkFill,
} from "@remixicon/react";

# Шаги и рабочие процессы

Шаги — фундаментальные строительные блоки Inngest, превращающие функции в надёжные рабочие процессы, способные выполняться месяцами и восстанавливаться после сбоев.

<CardGroup cols={1}>
  <Card title="Мышление категориями шагов" icon={<RiGuideFill className="text-basis h-4 w-4" />} href={'/docs/guides/multi-step-functions'}>
    Узнайте на примерах, как шаги обеспечивают более надёжные и гибкие функции с обработкой ошибок на уровне шагов, условными шагами и ожиданиями.
  </Card>
</CardGroup>


Освоив работу с шагами, можно добавлять новые возможности в функции Inngest:

<CardGroup cols={2}>
  <Card title="Добавить сны" icon={<RiCalendarLine className="text-basis h-4 w-4" />} href={'/docs/features/inngest-functions/steps-workflows/sleeps'}>
    Позвольте функциям Inngest приостанавливать выполнение от минут до месяцев.
  </Card>
  <Card title="Ожидание событий" icon={<EventIcon className="text-basis h-4 w-4"/>} href={'/docs/features/inngest-functions/steps-workflows/wait-for-event'}>
    Пишите функции, реагирующие на входящие события.
  </Card>
  <Card title="Циклы по шагам" icon={<RiGuideFill className="text-basis h-4 w-4" />} href={'/docs/guides/working-with-loops'}>
    Итерируйтесь по большим наборам данных с помощью шагов.
  </Card>
  <Card title="Параллельное выполнение шагов" icon={<RiGitForkFill className="rotate-90 text-basis h-4 w-4" />} href={'/docs/guides/step-parallelism'}>
    Узнайте, как применять паттерн map-reduce с помощью шагов.
  </Card>
</CardGroup>


## Как работают шаги

Может возникнуть вопрос: как работают шаги? Почему функция Inngest не завершается по таймауту при работе в бессерверной среде?

Можно думать о шагах как об API для описания контрольных точек в рабочем процессе — ожиданий или работы, которая выиграет от повторных попыток или параллелизма:

<CodeGroup>
```ts {{ title: "TypeScript" }}
inngest.createFunction(
  { id: "sync-systems", triggers: { event: "auto/sync.request" } },
  async ({ step }) => {
    // By wrapping code in step.run, the code will be retried if it throws an error and when successfuly.
    // It's result is saved to prevent unnecessary re-execution
    const data = await step.run("get-data", async () => {
      return getDataFromExternalSource();
    });

    // Can also be retried up to 4 times
    await step.run("save-data", async () => {
      return db.syncs.insertOne(data);
    });
  },
);
```
```py {{ title: "Python" }}
@inngest_client.create_function(
    fn_id="sync-systems",
    trigger=inngest.TriggerEvent(event="auto/sync.request"),
)
def sync_systems(ctx: inngest.ContextSync) -> None:
    # By wrapping code in step.run, the code will be retried if it throws an error and when successfuly.
    # It's result is saved to prevent unnecessary re-execution
    data = ctx.step.run("Get data", get_data_from_external_source)

    # Can also be retried up to 4 times
    ctx.step.run("Save data", db.syncs.insert_one, data)
```
```go {{ title: "Go" }}
!snippet:path=snippets/go/docs/functions/sync_systems_function.go
```
</CodeGroup>

Каждое выполнение шага зависит от взаимодействия с [Движком устойчивого исполнения](/docs/learn/how-functions-are-executed) Inngest, который отвечает за:

- Вызов функций с правильным состоянием шагов (текущий шаг + данные предыдущих шагов)
- Сбор результатов каждого шага и планирование следующего шага


<ImageTheme
  dark={'/assets/docs/features/inngest-functions/steps-workflows/Durable-Execution-Engine-steps-dark.jpg'}
  light={'/assets/docs/features/inngest-functions/steps-workflows/Durable-Execution-Engine-steps-light.jpg'}
  className="my-0"
  alt={'Each Inngest Functions\'s step invocation implies a communication between your application and the Inngest Platform. The illustration shows how each step results in two requests to the deployed application.'}
/>

Эта архитектура обеспечивает надёжность функций Inngest с повторяемыми шагами и ожиданиями от часов до месяцев. Также в бессерверной среде шаги получают расширенное максимальное время выполнения, позволяя создавать рабочие процессы, которые одновременно охватывают месяцы и выполняются более 5 минут!

Изучите следующее руководство для пошагового обзора полного выполнения рабочего процесса:

<CardGroup cols={1}>
  <Card title="Как выполняются функции" icon={<RiGuideFill className="text-basis h-4 w-4" />} href={'/docs/learn/how-functions-are-executed'}>
    Подробное погружение в Движок устойчивого исполнения Inngest с пошаговым примером выполнения рабочего процесса.
  </Card>
</CardGroup>

## Справочник SDK


<CardGroup cols={3}>

  <Card
    href={"/docs/reference/functions/step-run"}
    title={"TypeScript SDK"}
    icon={<TypeScriptIcon className="text-basis" />}
  >
  Справочник API шагов
  </Card>

  <Card
    href={"/docs/reference/python/steps/invoke"}
    title={"Python SDK"}
    icon={<PythonIcon className="text-basis" />}
  >
    Справочник API шагов
  </Card>


  <Card
    href={"https://pkg.go.dev/github.com/inngest/inngestgo@v0.9.0/step"}
    title={"Go SDK"}
    icon={<GoIcon className="text-basis" />}
  >
    Справочник API шагов
  </Card>

</CardGroup>