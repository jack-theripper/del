import { Callout, CodeGroup, Properties, Property, Row, Col, VersionBadge, LanguageSelector, LanguageSection } from "src/shared/Docs/mdx";

export const hidePageSidebar = true;


# Ошибки Inngest

Inngest автоматически обрабатывает ошибки и повторные попытки. Можно использовать стандартные ошибки или специальные ошибки Inngest для управления поведением при сбоях.

<LanguageSelector>


## Стандартные ошибки

<LanguageSection show="typescript">

Все объекты `Error` обрабатываются Inngest и [автоматически повторяются](/docs/features/inngest-functions/error-retries/retries). Это включает все стандартные ошибки, такие как `TypeError`, и пользовательские ошибки, расширяющие класс `Error`. Ошибки можно выбрасывать как в обработчике функции, так и внутри шага.

```typescript
export default inngest.createFunction(
  { id: "import-item-data", triggers: { event: "store/import.requested" } },
  async ({ event }) => {

    // throwing a standard error
    if (!event.itemId) {
      throw new Error("Item ID is required");
    }

    // throwing an error within a step
    const item = await step.run('fetch-item', async () => {
      const response = await fetch(`https://api.ecommerce.com/items/${event.itemId}`);
      if (response.status === 500) {
        throw new Error("Failed to fetch item from ecommerce API");
      }
      // ...
    });
  }
);
```
</LanguageSection>

<LanguageSection show="python">

Все выброшенные ошибки обрабатываются Inngest и [автоматически повторяются](/docs/features/inngest-functions/error-retries/retries). Это включает все стандартные ошибки, такие как `ValueError`, и пользовательские ошибки, расширяющие класс `Exception`. Ошибки можно выбрасывать как в обработчике функции, так и внутри шага.

```python
@client.create_function(
    fn_id="import-item-data",
    retries=0,
    trigger=inngest.TriggerEvent(event="store/import.requested"),
)
async def fn_async(ctx: inngest.Context) -> None:

    def foo() -> None:
        raise ValueError("foo")

    # a retry will be attempted
    await ctx.step.run("foo", foo)
```
</LanguageSection>

<LanguageSection show="go">

Все ошибки, возвращаемые функциями Inngest, обрабатываются Inngest и [автоматически повторяются](/docs/features/inngest-functions/error-retries/retries).

```go
import (
  "github.com/inngest/inngestgo"
  "github.com/inngest/inngestgo/step"
)

// Register the function
inngestgo.CreateFunction(
    client,
    inngestgo.FunctionOpts{
        ID: "send-user-email",
    },
    inngestgo.EventTrigger("user/created", nil),
    func(ctx context.Context, input inngestgo.Input[UserCreatedEvent]) (any, error) {
        // Run a step which emails the user.  This automatically retries on error.
        // This returns the fully typed result of the lambda.
        result, err := step.Run(ctx, "on-user-created", func(ctx context.Context) (bool, error) {
            // Run any code inside a step.
            result, err := emails.Send(emails.Opts{})
            return result, err
        })
        if err != nil {
            // This step retried 5 times by default and permanently failed.
            return nil, err
        }

        return result, nil
    },
)
```
</LanguageSection>

## Отключение повторных попыток

<LanguageSection show="typescript">

Используйте `NonRetriableError`, чтобы Inngest не повторял выполнение функции _или_ шага. Это полезно, когда ошибка не может быть устранена повторной попыткой — например, если она вызвана некорректными входными данными или ожидается снова при повторе.

```typescript
import { NonRetriableError } from "inngest";

export default inngest.createFunction(
  { id: "mark-store-imported", triggers: { event: "store/import.completed" } },
  async ({ event }) => {
    try {
      const result = await database.updateStore(
        { id: event.data.storeId },
        { imported: true }
      );
      return result.ok === true;
    } catch (err) {
      // Passing the original error via `cause` enables you to view the error in function logs
      throw new NonRetriableError("Store not found", { cause: err });
    }
  }
);
```

### Параметры

```ts
new NonRetriableError(message: string, options?: { cause?: Error }): NonRetriableError
```

<Properties>
  <Property name="message" type="string" required>
    Сообщение об ошибке.
  </Property>
  <Property name="options" type="object">
    <Properties nested={true} collapse={true}>
      <Property name="cause" type="Error">
        Исходная ошибка, вызвавшая невоспроизводимую ошибку.
      </Property>
    </Properties>
  </Property>
</Properties>

</LanguageSection>

<LanguageSection show="python">

Используйте `NonRetriableError`, чтобы Inngest не повторял выполнение функции _или_ шага. Это полезно, когда ошибка не может быть устранена повторной попыткой — например, если она вызвана некорректными входными данными или ожидается снова при повторе.

```python
@client.create_function(
    fn_id="import-item-data",
    retries=0,
    trigger=inngest.TriggerEvent(event="store/import.requested"),
)
async def fn_async(ctx: inngest.Context) -> None:
    def step_1() -> None:
        raise inngest.NonRetriableError("non-retriable-step-error")

    ctx.step.run("step_1", step_1)
```

</LanguageSection>

<LanguageSection show="go">

Используйте `inngestgo.NoRetryError`, чтобы предотвратить повторное выполнение функции Inngest. Это полезно, когда ошибка не может быть устранена повторной попыткой — например, если она вызвана некорректными входными данными или ожидается снова при повторе.

```go
import (
  "github.com/inngest/inngestgo"
  "github.com/inngest/inngestgo/step"
)

// Register the function
inngestgo.CreateFunction(
    client,
    inngestgo.FunctionOpts{
        ID: "send-user-email",
    },
    inngestgo.EventTrigger("user/created", nil),
    func(ctx context.Context, input inngestgo.Input[UserCreatedEvent]) (any, error) {
        // Run a step which emails the user.  This automatically retries on error.
        // This returns the fully typed result of the lambda.
        result, err := step.Run(ctx, "on-user-created", func(ctx context.Context) (bool, error) {
            // Run any code inside a step.
            result, err := emails.Send(emails.Opts{})
            return result, err
        })
        if err != nil {
            // This step retried 5 times by default and permanently failed.
            // we return a NoRetryError to prevent Inngest from retrying the function
            return nil, inngestgo.NoRetryError(err)
        }

        return result, nil
    },
)
```

</LanguageSection>

## Повтор через определённый период времени

<LanguageSection show="typescript">

Используйте `RetryAfterError`, чтобы контролировать, когда Inngest должен повторить выполнение функции или шага. Это полезно, когда нужно отложить следующую попытку на определённое время — например, для более корректной обработки гонки условий или отступа после превышения лимита запросов к API.

Если `RetryAfterError` не используется, Inngest применяет [политику повторов с экспоненциальной задержкой по умолчанию](https://github.com/inngest/inngest/blob/main/pkg/backoff/backoff.go#L10-L22).

```typescript
inngest.createFunction(
  { id: "send-welcome-sms", triggers: { event: "app/user.created" } },
  async ({ event, step }) => {
    const { success, retryAfter } = await twilio.messages.create({
      to: event.data.user.phoneNumber,
      body: "Welcome to our service!",
    });

    if (!success && retryAfter) {
      throw new RetryAfterError("Hit Twilio rate limit", retryAfter);
    }
  }
);
```

### Параметры

```ts
new RetryAfterError(
  message: string,
  retryAfter: number | string | date,
  options?: { cause?: Error }
): RetryAfterError
```

<Properties>
  <Property name="message" type="string" required>
    Сообщение об ошибке.
  </Property>
  <Property name="retryAfter" type="number | string | date" required>
    Указанное время задержки следующей попытки. Допустимые форматы:

    * `number` — количество **миллисекунд** задержки.
    * `string` — строка времени, разбираемая пакетом [ms](https://npm.im/ms), например `"30m"`, `"3 hours"` или `"2.5d"`.
    * `date` — объект `Date`.
  </Property>
  <Property name="options" type="object">
    <Properties nested={true} collapse={true}>
      <Property name="cause" type="Error">
        Исходная ошибка, вызвавшая невоспроизводимую ошибку.
      </Property>
    </Properties>
  </Property>
</Properties>

</LanguageSection>

<LanguageSection show="python">

Используйте `RetryAfterError`, чтобы контролировать, когда Inngest должен повторить выполнение функции или шага. Это полезно, когда нужно отложить следующую попытку на определённое время — например, для более корректной обработки гонки условий или отступа после превышения лимита запросов к API.

Если `RetryAfterError` не используется, Inngest применяет [политику повторов с экспоненциальной задержкой по умолчанию](https://github.com/inngest/inngest/blob/main/pkg/backoff/backoff.go#L10-L22).

```python
@client.create_function(
    fn_id="import-item-data",
    retries=0,
    trigger=inngest.TriggerEvent(event="store/import.requested"),
)
async def fn_async(ctx: inngest.Context) -> None:
  def step_1() -> None:
      raise inngest.RetryAfterError("rate-limit-hit", 1000) # delay in milliseconds

  ctx.step.run("step_1", step_1)
```

### Параметры

```python
RetryAfterError(
  message: typing.Optional[str],
  retry_after: typing.Union[int, datetime.timedelta, datetime.datetime],
) -> None
```

<Properties>
  <Property name="message" type="string" required>
    Сообщение об ошибке.
  </Property>
  <Property name="retry_after" type="int | datetime.timedelta | datetime.datetime" required>
    Указанное время задержки следующей попытки. Допустимые форматы:

    * `int` — количество **миллисекунд** задержки.
    * `datetime.timedelta` — объект timedelta, например `datetime.timedelta(seconds=30)`.
    * `datetime.datetime` — объект datetime.
  </Property>
</Properties>

</LanguageSection>

<LanguageSection show="go">

Используйте `RetryAtError`, чтобы контролировать, когда Inngest должен повторить выполнение функции или шага. Это полезно, когда нужно отложить следующую попытку на определённое время — например, для более корректной обработки гонки условий или отступа после превышения лимита запросов к API.

Если `RetryAtError` не используется, Inngest применяет [политику повторов с экспоненциальной задержкой по умолчанию](https://github.com/inngest/inngest/blob/main/pkg/backoff/backoff.go#L10-L22).

```go
import (
  "github.com/inngest/inngestgo"
  "github.com/inngest/inngestgo/step"
)

// Register the function
inngestgo.CreateFunction(
    client,
    inngestgo.FunctionOpts{
        ID: "send-user-email",
    },
    inngestgo.EventTrigger("user/created", nil),
    func(ctx context.Context, input inngestgo.Input[UserCreatedEvent]) (any, error) {
        // Run a step which emails the user.  This automatically retries on error.
        // This returns the fully typed result of the lambda.
        result, err := step.Run(ctx, "on-user-created", func(ctx context.Context) (bool, error) {
            // Run any code inside a step.
            result, err := emails.Send(emails.Opts{})
            return result, err
        })
        if err != nil {
            // This step retried 5 times by default and permanently failed.
            // We delay the next retry attempt by 5 hours
            return nil, inngestgo.RetryAtError(err, time.Now().Add(5*time.Hour))
        }

        return result, nil
    },
)
```

</LanguageSection>

<LanguageSection show="typescript">

## Ошибки шагов <VersionBadge version="v3.12.0+" />

После того как шаг исчерпал все повторные попытки, он выбрасывает `StepError`, который можно перехватить и обработать в обработчике функции.

<CodeGroup>
```ts {{ title: "try/catch" }}
inngest.createFunction(
  { id: "send-weather-forecast", triggers: { event: "weather/forecast.requested" } },
  async ({ event, step }) => {
    let data;

    try {
      data = await step.run('get-public-weather-data', async () => {
        return await fetch('https://api.weather.com/data');
      });
    } catch (err) {
      // err will be an instance of StepError
      // Handle the error by recovering with a different step
      data = await step.run('use-backup-weather-api', async () => {
        return await fetch('https://api.stormwaters.com/data');
      });
    }
    // ...
  }
);
```
```ts {{ title: "Chaining with .catch()" }}
inngest.createFunction(
  { id: "send-weather-forecast", triggers: { event: "weather/forecast.requested" } },
  async ({ event, step }) => {

    const data = await step
      .run('get-public-weather-data', async () => {
        return await fetch('https://api.example.com/data');
      })
      .catch((err) => {
        // err will be an instance of StepError
        // Recover with a chained step
        return step.run("use-backup-weather-api", () => {
          return await fetch('https://api.stormwaters.com/data');
        });
      });
  }
);
```
```ts {{ title: "Ignoring and logging the error" }}
inngest.createFunction(
  { id: "send-weather-forecast", triggers: { event: "weather/forecast.requested" } },
  async ({ event, step }) => {

    const data = await step
      .run('get-public-weather-data', async () => {
        return await fetch('https://api.example.com/data');
      })
      // This will swallow the error and log it if it's non critical
      .catch((err) => logger.error(err));
  }
);
```
</CodeGroup>

<Callout>
  Поддержка обработки ошибок шагов доступна в TypeScript SDK Inngest начиная с версии **3.12.0**. В более ранних версиях оборачивание шага в try/catch не работает корректно.
</Callout>

</LanguageSection>

<LanguageSection show="python">

## Ошибки шагов

После того как шаг исчерпал все повторные попытки, он выбрасывает `StepError`, который можно перехватить и обработать в обработчике функции.

```python
@client.create_function(
    fn_id="import-item-data",
    retries=0,
    trigger=inngest.TriggerEvent(event="store/import.requested"),
)
async def fn_async(ctx: inngest.Context) -> None:
    def foo() -> None:
        raise ValueError("foo")

    try:
        ctx.step.run("foo", foo)
    except inngest.StepError:
        raise MyError("I am new")
```

</LanguageSection>

<LanguageSection show="typescript">

## Счётчик попыток

Текущий номер попытки передаётся в обработчик функции как входной параметр. `attempt` — это индексируемое с нуля число, которое увеличивается при каждом повторе. Первая попытка будет `0`, вторая `1` и так далее. Счётчик сбрасывается после успешного выполнения шага.

```ts
inngest.createFunction(
  { id: "generate-summary", triggers: { event: "blog/post.created" } },
  async ({ attempt }) => {
    // `attempt` is the zero-index attempt number

    await step.run('call-llm', async () => {
      if (attempt < 2) {
        // Call OpenAI's API two times
      } else {
        // After two attempts to OpenAI, try a different LLM, for example, Mistral
      }
    });
  }
);
```


## Трассировки стека

При вызове функций, возвращающих Promise, используйте `await`, чтобы сохранить трассировку стека. Это применяется к функциям, выполняющимся в разных циклах цикла событий, например при обращении к базе данных или внешнему API. Особенно полезно при отладке ошибок в production.

<CodeGroup>
  ```ts {{ title: "Returning Promise" }}
  inngest.createFunction(
    { id: "update-recent-usage", triggers: { event: "app/update-recent-usage" } },
    async ({ event, step }) => {
      // ...
      await step.run("update in db", () => doSomeWork(event.data));
      // ...
    }
  );
  ```
  ```ts {{ title: "Awaiting Promise" }}
  inngest.createFunction(
    { id: "update-recent-usage", triggers: { event: "app/update-recent-usage" } },
    async ({ event, step }) => {
      // ...
      await step.run("update in db", async () => {
        return await doSomeWork(event.data);
      });
      // ...
    }
  );

  ```
</CodeGroup>

Обратите внимание: при немедленном возврате Promise трассировка стека не будет включать указатель на вызывающую функцию. Использование `await` гарантирует, что трассировка стека будет содержать вызывающую функцию.

</LanguageSection>
</LanguageSelector>